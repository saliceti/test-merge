name: Test workflow

on:
  push:
    branches:
      - main
  pull_request:


jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:

      - name: test step
        shell: bash
        run: |
          echo bonjour
